bool isPalindrome(int x){
    if(x<0)
        return false;
    long int reverse = 0;
    int digit = x;
    
    while(digit){
     reverse = reverse*10+digit%10;
     digit/=10;
    }
        
return (x == reverse);
}
